{
  "name": "smsautoresponder",
  "version": "1.0.0",
  "description": "SMS Auto Responder is a service that responds to incoming text message pre-configured responses.",
  "type": "module",
  "scripts": {
    "start": "func start",
    "coverage": "vitest run --coverage",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.16.0",
    "twilio": "^4.18.0"
  },
  "main": "src/index.js",
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.6",
    "vitest": "^0.34.6",
    "dotenv": "^16.3.1",
    "vite": "^4.4.10"
  },
  "engines": {
    "node": "^20.0.0"
  }
}